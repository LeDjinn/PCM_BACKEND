<%= form_with(model: matter) do |form| %>
  <% if matter.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(matter.errors.count, "error") %> prohibited this matter from being saved:</h2>

      <ul>
        <% matter.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price_local, style: "display: block" %>
    <%= form.text_field :price_local %>
  </div>

  <div>
    <%= form.label :price_export, style: "display: block" %>
    <%= form.text_field :price_export %>
  </div>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
